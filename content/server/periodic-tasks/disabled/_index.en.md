+++
title = "Disabled by Default"
weight = 30
+++

This section describes tasks that are not enabled by default.

You can add [sections](/server/periodic-tasks/configuring-tasks/) for them in your Config.toml file to enable them.

> There is no need to explicitly set "enabled" to "true". Specifying the name of a task is enough to enable it.
